## Remove Tasks Subtitle

In `src/components/tasks/ProjectTasksTab.tsx`, remove the subtitle paragraph that appears directly below the "Tasks" heading (lines 184-187), leaving only the `<h2>Tasks</h2>` heading.

### Files to edit
- `src/components/tasks/ProjectTasksTab.tsx` — delete the `<p>` element containing the "Smart Task Intelligence" description.